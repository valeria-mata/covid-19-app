export interface UserData {
    name: string;
    phone: string;
    email: string;
    birthyear: string;
    regdate: Date;
}

export interface userActivation {
    telephone: string;
    activationCode: string;
}
