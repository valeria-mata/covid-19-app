export interface UserData {
    name: string;
    phone: string;
    email: string;
    birthyear: string;
    regdate: string;
}

export interface userActivation {
    telephone: string;
    activationCode: string;
}
