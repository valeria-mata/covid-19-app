export interface UserData {
    name: string;
    phone: string;
    email: string;
    birthyear: string;
    registrationDate: Date;
}

export interface userActivation {
    telephone: string;
    activationCode: string;
}
